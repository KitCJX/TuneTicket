{
  "name": "spotifysongshare",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "lint": "eslint .",
    "check": "npm run lint && npm test && npm run build"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "html2canvas": "^1.4.1"
  }
}
